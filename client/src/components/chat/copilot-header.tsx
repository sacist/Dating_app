import { MainText } from "../../UI/main-text"
import { Header } from "../../UI/chat/copilot"
export const CopilotHeader=() => {
    return(
        <Header>
            <MainText $color="darkblue">Индивидуальный ИИ помощник</MainText>
        </Header>
    )
}