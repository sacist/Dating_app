import { ChatListWrapper } from "../../UI/chat/chat-list";

//Тут рендерим список компонентов Chat и вызываем api на fetch чатов


export const ChatList=() => {
    return(
        <ChatListWrapper>

        </ChatListWrapper>
    )
}