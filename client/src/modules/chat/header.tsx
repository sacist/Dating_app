import { ChatHeader } from "../../UI/chat/header";
import { MatchmakingWindow } from "./matchmaking-window";
export const Header=() => {
    return(
        <ChatHeader>
            <MatchmakingWindow/>
        </ChatHeader>
    )
}